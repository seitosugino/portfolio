<% if @cart_items.present? %>
  $('.cart').remove();
  $('.total_price').html("合計 <%= @total.round.to_s(:delimited) %>円");
<% else %>
  $('.no_cart').html("<%= j(render 'cart_items/no_cart') %>");
<% end %>
  $('.flash').html("<%= j(render 'layouts/flash') %>");